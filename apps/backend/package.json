{
  "name": "@clicky/backend",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@clicky/backend:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@clicky/backend:build:development"
          },
          "production": {
            "buildTarget": "@clicky/backend:build:production"
          }
        }
      }
    }
  }
}
